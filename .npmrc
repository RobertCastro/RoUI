# Mapea el scope @robertcastro a GitHub Packages (seguro de versionar — sin secretos).
# El token de autenticación NO va aquí; ponlo en tu ~/.npmrc (usuario) o en CI:
#   //npm.pkg.github.com/:_authToken=TU_TOKEN
@robertcastro:registry=https://npm.pkg.github.com
