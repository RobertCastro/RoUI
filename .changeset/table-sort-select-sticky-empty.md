---
"@robertcastro/roui": minor
---

Table: cabecera ordenable (`.ro-table__sort` + contrato `aria-sort`), selección de filas (`.ro-table__select`), header sticky con scroll vertical acotado (`.ro-table--sticky` + `.ro-table-wrap--scroll`) y estado vacío integrado (`.ro-table__empty-cell`).
