# CSS y HTML quedan fuera del alcance de Prettier: ambos son markup/estilo
# denso escrito a mano (demos, plantillas, componentes) donde el formato por
# defecto de Prettier (una propiedad por línea en CSS; auto-cierre de void
# elements y saltos de línea dentro de contenido inline en HTML) reescribe el
# archivo de forma invasiva sin aportar valor real, y en CSS además haría
# crecer el presupuesto de paquete sin necesidad. stylelint sigue gobernando
# la calidad del CSS.
*.css
*.html

# Generado, nunca editado a mano.
dist/
tokens/tailwind.preset.cjs
CHANGELOG.md
package-lock.json

# Artefactos de build/test.
node_modules/
test-results/
playwright-report/
coverage/
